const initialState = {
  user: null,
  theme: "light",
  lang: "TR",
};

export default function userReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
